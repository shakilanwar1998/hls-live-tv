{
  "name": "hls-live-player",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency web app to play live HLS (.m3u8) streams, with a built-in CORS proxy.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
